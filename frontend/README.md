# Frontend (Vite + React) quickstart

1. Install dependencies:
   cd frontend
   npm install

2. Run dev server:
   npm run dev

## Notes

- Components placeholders created: CSVImporter, CardTable, TemplateEditor, LivePreview, ExportButton.
- Replace placeholder implementations with API calls to the backend.
